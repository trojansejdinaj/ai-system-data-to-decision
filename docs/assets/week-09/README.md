# Week 09 Evidence

Add screenshots/files here to prove the end-to-end flow.

## Required screenshots
1) `01-runs-clean-metrics.png`
   - terminal output from:
     - `make clean`
     - `make metrics`
     - `make runs`
   - must show `clean | succeeded` and `metrics | succeeded`

2) `02-metrics-output.png`
   - terminal output row from `make metrics`
   - should show `month_start`, `total_records`, etc.

3) (Optional) `03-dashboard.png`
   - browser screenshot of `http://localhost:8000/dashboard`

## Files
- Flags CSV is generated at:
  - `docs/assets/week-07/flags_report.csv`
